sudo: required 
# Enable C++ support
language: cpp

# Compiler selection
compiler:
  - gcc
  

# Build steps
script:
  - sudo apt-get install libxorg-gtest-dev
  - cd USV_autopilot_project/Debug
  - ls
  - make
  - cd ..
  - cd ..
  - cd UnitTest/Debug
  - ls
  - make all

notifications:
email: false
