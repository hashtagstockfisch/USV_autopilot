sudo: required 
# Enable C++ support
language: cpp

# Compiler selection
compiler:
  - gcc
  

# Build steps
script:
  - sudo apt-get install libxorg-gtest-dev
  - cd USV_autopilot_project/Debug
  - make
  - cd ..
  - cd ..
  - cd UnitTest/Debug
  - make all

notifications:
email: false
